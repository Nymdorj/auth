{% extends "base.html.twig" %}
{% block title %}New Post{% endblock %}
{% block body %}
    <div class="text-left">
        <h3 class="mb-2">New Post</h3>
        {{ form_start(form) }}
        {{ form_widget(form.title) }}
            <div class="mt-2">
                <button type="submit" value="save" class="btn btn-primary">Create</button>
                <a href="/profile" value="cancel" class="btn btn-default ml-2">Cancel</a>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}